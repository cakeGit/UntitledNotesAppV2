export function EmptyPageHint({pageRef}) {
    return (
        <div className="empty_page_hint">
            <h2>This page is empty :&#40;</h2>
            <p>Use the "+" button to add blocks to the page.</p>
        </div>
    );
}